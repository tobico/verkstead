# The human's Response to examples/questions.yaml — what the web UI will
# submit, and what the quickstart submits by hand:
#
#   curl -X POST --data-binary @examples/response.yaml \
#     -H 'Content-Type: application/yaml' \
#     http://127.0.0.1:8422/conversations/1/api/v1/sets/1/response
#
# The agents' API hangs off the Conversation the Set was asked from — that is
# what the base URL a session is given says — so answering one is reached the
# same way. The web UI posts to `/api/ui/sets/1/response` instead, which is the
# human's half of the same store.
#
# Every Question and Sub-question in the Set appears exactly once — Q1, Q2,
# Q2a and Q3 — each with a selected Option, free text, both, or the
# `unanswered` marker. Leaving a question open is legal; leaving it out is
# not.
#
# The Set's Postscript is not a Question and takes no Answer: what it prompted
# lands in the set-level `comment` at the bottom.

answers:
  - label: Q1
    selected: 1
    free_text: >-
      Start in-process. Revisit it the day we run more than two instances.

  - label: Q2
    selected: 2

  # Explicitly still open: the agent must not read this as a preference.
  - label: Q2a
    unanswered: true

  # The one fact the agent had no way to guess.
  - label: Q3
    free_text: acct_8f21c3, the nightly export job in `ops/export`.

comment: |
  Ship it behind a flag and turn it on for the one noisy client first.

  That export job hammers the endpoint on purpose, so give it an allowlist
  entry rather than a bigger bucket for everyone.

  On Q2a I genuinely don't know — pick whatever our SDK's retry logic
  already understands, and say in the PR which one that was.
